import { StyleSheet } from "react-native-web";

const styles = StyleSheet.create({
    container: {
        flex: 0.3,
    },
    switch: {
        flexDirection: 'row'
    },
    titulo: {
        fontSize: '20px'
    }
})

export {styles}